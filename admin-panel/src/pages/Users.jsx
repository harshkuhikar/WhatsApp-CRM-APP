import { Typography } from '@mui/material';

function Users() {
  return (
    <div>
      <Typography variant="h4">User Management</Typography>
      <Typography color="text.secondary">Manage all users</Typography>
    </div>
  );
}

export default Users;
